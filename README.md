### Generate the ASCII art
Fastfetch is configured to read the logo from a static file. This bypasses JSON "Invalid Enum" errors and improves terminal startup speed.

```bash
pokemon-colorscripts -n gengar --no-title > ~/.gengar_ascii
```
