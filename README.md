### Generate the ASCII art
Feel free to change the pokemon or the file name, just make sure you change the file name in the fastfetch as well if you do so
```bash
pokemon-colorscripts -n gengar --no-title > ~/.gengar_ascii
```
